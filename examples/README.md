# Example projects

This directory contains isolated sample projects by build tool:

- `spring-boot-maven/`
- `spring-boot-gradle/`

These are intentionally minimal and are the preferred examples for build-tool-specific walkthroughs.

The legacy mixed sample under `samples/java-spring-docker/` is still used for benchmark assets and backward compatibility.
